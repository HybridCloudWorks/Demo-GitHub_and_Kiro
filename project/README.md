# My Learning Journal — the course project

This is the real web app you build across the course. It's a **static site** — plain HTML, CSS,
and JavaScript with **no build tools** — so it runs by simply opening `index.html` in a browser.

## Run it locally

You have two easy options:

- **Just open it:** double-click `index.html`, or drag it into a browser.
- **Serve it** (closer to how it runs on the web) with any static server, e.g.:

  ```bash
  # Python (built in on most systems)
  python3 -m http.server 8000
  # then visit http://localhost:8000
  ```

## Files

| File | Purpose |
|------|---------|
| `index.html` | The page structure and content. |
| `styles.css` | The styling. |
| `app.js` | A small script (sets the footer year). |

## Milestones

The project grows one level at a time. Each stage is tagged in Git:

| Tag | Added in | What's new |
|-----|----------|------------|
| `milestone-l2` | Level 2 | The initial static page. |
| `milestone-l3` | Level 3 | An About section added via a pull request. |
| `milestone-l4` | Level 4 | Automated deployment workflow to GitHub Pages. |
| `milestone-l5` | Level 5 | A Contact section added spec-driven with Kiro. |
| `milestone-l7` | Level 7 | A Projects section — the deployed, protected, automated capstone. |
