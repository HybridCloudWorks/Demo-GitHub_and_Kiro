# Tasks — Contact Section

- [x] 1. Add the Contact section markup to `project/index.html`
  - Insert `<section class="card" id="contact">` after the About section.
  - Include an `<h2>Contact</h2>` and a paragraph with a `mailto:` link.
  - _Requirements: 1.1, 1.2, 1.3, 2.1_

- [x] 2. Verify styling and conventions
  - Confirm the section uses the `.card` class and matches the site style.
  - Ensure no build tools/frameworks were added.
  - _Requirements: 2.1, 2.3_

- [x] 3. Validate
  - Run `node project/scripts/check-static-site.js` and confirm it passes.
  - _Requirements: 2.2_

- [x] 4. Commit as the L5 milestone
  - Commit with an imperative message and tag `milestone-l5`.
  - _Requirements: all_
