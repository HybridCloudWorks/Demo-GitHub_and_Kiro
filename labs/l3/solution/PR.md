# My Pull Request

**Title:** Add About section to the homepage

**Branch:** add-about-section

Fixes #1

## Description

Adds a new `<section id="about">` to `index.html` with a heading and a short
paragraph introducing myself. Built on a feature branch and opened as a pull
request so the change is reviewed and checks pass before merging into `main`.
