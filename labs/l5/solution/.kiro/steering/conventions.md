---
inclusion: always
---

# My project conventions

- This project is a **static site** — plain HTML, CSS, and JavaScript.
- Do not add build tools or frameworks.
- Keep styles in `styles.css` and reuse existing CSS custom properties.
- Run the static-site check before finishing a change.
