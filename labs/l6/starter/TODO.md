# TODO

Create `.kiro/settings/mcp.json` in this starter folder to complete the lab.

See the lab README one level up for the exact requirements, and `examples/mcp.json` for a
reference. When done, the file path will be:

```text
labs/l6/starter/.kiro/settings/mcp.json
```
